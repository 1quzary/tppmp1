import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

{
	/*Лабораторна 1*/
}
{
	/*
export default function FourthScreen() {
	let integerNumber // undefined

	let decimalNumber // undefined

	integerNumber = 10

	integerNumber++ // 11

	integerNumber = -integerNumber // -11

	// 6. Присвоюємо decimalNumber значення integerNumber
	decimalNumber = integerNumber

	let pairOfValues = {
		integerNumber,
		decimalNumber,
	}

	let integerOutput =
		pairOfValues.integerNumber !== undefined
			? `Integer value: ${pairOfValues.integerNumber}`
			: 'Integer is undefined'

	let decimalOutput =
		pairOfValues.decimalNumber !== undefined
			? `Decimal value: ${pairOfValues.decimalNumber}`
			: 'Decimal is undefined'

	// Optional binding аналог через if
	let optionalDecimalOutput
	if (decimalNumber !== undefined) {
		optionalDecimalOutput = `Optional binding: ${decimalNumber}`
	} else {
		optionalDecimalOutput = 'Decimal is undefined'
	}

	return (
		<View style={styles.container}>
			<Text style={styles.title}>Завдання 4</Text>

			<Text style={styles.text}>1. integerNumber: undefined</Text>
			<Text style={styles.text}>2. decimalNumber: undefined</Text>
			<Text style={styles.text}>3. integerNumber = 10</Text>
			<Text style={styles.text}>4. Після increment: 11</Text>
			<Text style={styles.text}>5. Після unary minus: -11</Text>
			<Text style={styles.text}>6. decimalNumber = integerNumber (-11)</Text>
			<Text style={styles.text}>7. Створено pairOfValues</Text>
			<Text style={styles.text}>8. {integerOutput}</Text>
			<Text style={styles.text}>9. {decimalOutput}</Text>
			<Text style={styles.text}>10. {optionalDecimalOutput}</Text>
		</View>
	)
}

const styles = StyleSheet.create({
	container: {
		flex: 1,
		alignItems: 'left',
		justifyContent: 'top',
		padding: 16,
	},
	title: {
		fontSize: 22,
		marginBottom: 16,
		fontWeight: 'bold',
	},
	text: {
		fontSize: 16,
		marginBottom: 6,
		textAlign: 'left',
	},
})
	*/
}

{
	/*Лабораторна 3*/
}
export default function FourthScreen() {}

const styles = StyleSheet.create({})
